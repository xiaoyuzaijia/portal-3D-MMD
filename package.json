{
  "name": "head-tracked-3d-v2",
  "version": "1.0.0",
  "description": "Glasses-free 3D display — head tracking (MediaPipe) + calibration + MMD support + Three.js",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.170.0",
    "tweakpane": "^4.0.5"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "@types/three": "^0.170.0",
    "typescript": "^5.8.0",
    "vite": "^6.3.0"
  }
}
